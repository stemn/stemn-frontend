// export const getPageData = (allData, page, size) => {
//  const start = page * size || 0;
//  const end = start + size;
//  return allData && allData.length > 0
//    ? allData.slice(start, end)
//    : allData
// }
