import i from 'icepick';

const initialState = {
  data: {},
  projects: {}
}

const mainReducer = (state, action) => {
  switch (action.type) {
    case 'MENTIONS/':
      return state
    default:
      return state;
  }
}
