// import _ from 'lodash';
//
// export default (functions, standardInput) => {
//  return _.map(functions, (item)=>{
//    if(_.isFunction(item)){
//      return (input1)=>{
//        const extendedInput = Object.assign({}, standardInput, input1);
//        return item(extendedInput)
//      }
//    }
//  })
// }
