export const elByPh = (el, placeholder) => `${el}[placeholder='${placeholder}']`
export const elByTitle = (el, title) => `${el}[title='${title}']`
export const elByDataTag = tag => `[data-tag='${tag}']`
