export const elByPh = (el: string, placeholder: string) => `${el}[placeholder='${placeholder}']`
export const elByTitle = (el: string, title: string) => `${el}[title='${title}']`
export const elByDataTag = (tag: string) => `[data-tag='${tag}']`
