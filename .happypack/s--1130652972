'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.name = undefined;
exports.default = reducer;

var _icepick = require('icepick');

var _icepick2 = _interopRequireDefault(_icepick);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var name = exports.name = 'autoLaunch';

var initialState = {
  status: false
};

function reducer() {
  var state = arguments.length <= 0 || arguments[0] === undefined ? initialState : arguments[0];
  var action = arguments.length <= 1 || arguments[1] === undefined ? {} : arguments[1];

  switch (action.type) {
    case 'AUTO_LAUNCH/GET_STATUS_FULFILLED':
      return _icepick2.default.assoc(state, 'status', action.payload);
    case 'AUTO_LAUNCH/TOGGLE_FULFILLED':
      return _icepick2.default.assoc(state, 'status', action.meta.status);
    default:
      return state;
  }
}